// This file serves as our color 'hub' so we will set all the colors we will use in the app here
// If we will need to change some color, we will need to change it only at 1 place instead of in all components
const Colors = {
  primary500: "#72063c",
  primary600: "#640233",
  primary700: "#4e0329",
  primary800: "#3b021f",
  accent500: "#ddb52f",
};

export default Colors;
